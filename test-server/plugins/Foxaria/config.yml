environment: dev
debug: false
broadcast:
  # Жёлтые сообщения «игрок зашёл / вышел» в чате
  hide-vanilla-join-quit: true
# Скопируй database (строки type..password) в plugins/FoxariaProxy/config.yml → moderation.backend-database, чтобы Bungee /unpunish снимал баны из той же MySQL, что Paper.
database:
  type: mysql
  file: foxaria.db
  host: 127.0.0.1
  port: 3306
  name: foxaria
  username: foxaria
  password: Grnkbq2GS7rBj71TWmx
  pool:
    maximum-size: 10
    minimum-idle: 2
  executor-threads: 4
teleport-warmup:
  ticks: 60
combat-tag:
  duration-seconds: 15
  kill-on-logout: true
spawn-protection:
  enabled: true
  world: world
  radius: 64
  block-chorus-into-spawn: true
homes:
  default-limit: 2
tpa:
  request-expiry-seconds: 60
rtp:
  world: world
  radius: 5000
  min-distance: 200
  max-attempts: 24
nether-roof-protection:
  enabled: true
  max-y: 127.0
lag-protection:
  cleanup-interval-ticks: 1200
  item-despawn-ticks: 2400
  chunk-entity-limit: 180
economy:
  transfer-fee-percent: 2.5
